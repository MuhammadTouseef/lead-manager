import React from 'react';
import PropTypes from 'prop-types';

Leads.propTypes = {
    
};

function Leads(props) {
    return (
        <h1>
            Leads
        </h1>
    );
}

export default Leads;