import React from 'react';
import PropTypes from 'prop-types';

Form.propTypes = {
    
};

function Form(props) {
    return (
        <h1>
            Form
        </h1>
    );
}

export default Form;